# Saga X Agent Desk — runtime dependencies
#
# The project began as "no pip install, stdlib only". That held while the
# desk was a passive dashboard. Giving the agents a brain and a database
# ends it. Two packages, both load-bearing:

# Postgres driver. server.py talks to the desk schema directly rather
# than through PostgREST, so the schema is never exposed to the internet.
# [binary] ships prebuilt wheels — no libpq build step on the VPS.
psycopg[binary]>=3.2

# OpenAI SDK for the agent layer (gpt-5.6-sol).
openai>=1.0

# Install:  python -m pip install -r requirements.txt
